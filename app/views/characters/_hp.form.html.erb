<%= form_for ([@character, @hp]) do |f| %>
  <% if @hp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hp.errors.count, "error") %> prohibited this hp from                 
        being saved:</h2>

      <ul>
        <% @hp.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :total_hp %><br />
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :current_hp %><br />
    <%= f.text_field :current %>
  </div>
  <div class="field">
    <%= f.label :nonlethal_damage %><br />
    <%= f.text_field :nonlethal %>
  </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>