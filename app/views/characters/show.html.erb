<%= render 'menu' %>

<p id="notice"><%= notice %></p>

<%= link_to 'Create Items Page', character_items_path(@character) unless @character.items.exists? %>
<br />
<%= link_to 'Create Special Abilities Page', character_specials_path(@character) unless @character.specials.exists? %>


<p>
<%= render 'details' %>
</p>

<p>
<%= render 'statistics' %>
</p>

<p>
<%= render 'hp' %>
</p>

<p>
<%= render 'ac' %>
</p>

<p>
<%= render 'initiative' %>
</p>

<p>
<%= render 'saves' %>
</p>

<p>
<%= render 'background' %>
</p>
