<%= form_for ([@character, @hp]) do |f| %>
<% if @hp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hp.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
        <% @hp.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br />
    <%= f.text_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
